name: Python CI 
on:
  - push 
jobs: 
  test: 
    runs-on: ubuntu-latest 
    steps:
    - uses: actions/checkout@v2 
    - uses: actions/setup-python@v2
      with:
          python-version: '3.6'
    - run: pip3 install -r requirements.txt
    - run: python3 HelloWorldTest.py
